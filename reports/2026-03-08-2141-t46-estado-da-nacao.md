# Estado da Nacao - Tijolo 46

Data: 2026-03-09 00:41  
Status: concluido  
Escopo: hardening operacional do pipeline de assets

## 1. Objetivo

Endurecer o pipeline de assets para operacao confiavel, reduzindo ruido de orfaos, formalizando politica de auditoria e deixando gate de CI previsivel para escala.

## 2. Diagnostico de entrada

Estado inicial observado no T46:
- auditoria funcional, mas com falso positivo massivo em Windows por separador de path (`\\` vs `/`);
- output com dezenas de orfaos inexistentes, reduzindo confianca no audit;
- 3 orfaos reais no `mutirao-do-bairro` (variantes v1 legadas) escondidos no ruido;
- ausencia de politica formal unica para tratar warning/error e legados.

## 3. Entregas realizadas

### 3.1 Correcao critica no audit

Arquivo: `tools/assets-audit.js`

- implementada normalizacao cross-platform de paths (`\\` -> `/`);
- comparacao manifest vs disco corrigida;
- resultado: falsos positivos eliminados e estado real recuperado.

### 3.2 Politica formal de auditoria

Arquivo: `docs/politica-de-auditoria-de-assets.md`

Definido:
- severidades oficiais (`ok`, `warning`, `error`);
- criterios de orfaos (allowed, legacy safe, unexpected debt);
- fluxo operacional de integracao (`gerar -> pasta -> manifest -> audit -> merge`);
- frequencia de higiene e responsabilidades.

### 3.3 Allowlist operacional

Arquivo: `tools/assets-audit-allowlist.json`

- criado formato oficial de allowlist com justificativa por arquivo;
- adicionados 3 legados v1 do mutirao como rollback safety com data de review:
  - `ui-action-reparar-v1.svg`
  - `ui-action-defender-v1.svg`
  - `ui-action-mobilizar-v1.svg`

### 3.4 Integracao do allowlist no audit

Arquivo: `tools/assets-audit.js`

- carregamento opcional de allowlist por game slug;
- orfao allowlisted nao gera warning;
- comportamento backward-compatible para jogos sem allowlist.

### 3.5 Gate de CI refinado

Arquivo: `tools/assets-audit.js`

- regra final de exit code:
  - `error` => exit 1 (bloqueia merge/deploy)
  - `warning`/`ok` => exit 0 (permite merge/deploy)
- contabilizacao de erro agora baseada em severidade real (`issue.severity === 'error'`), nao apenas missingAssets.

### 3.6 Documento de gate

Arquivo: `docs/ci-gate-assets.md`

- contrato oficial de pre-merge/CI;
- matriz de comportamento por severidade;
- exemplo de workflow GitHub Actions;
- politica de escalacao e manutencao.

### 3.7 Workflow docs atualizados

Arquivo: `docs/pipeline-de-assets-do-hub.md`

- secao T46 adicionada com:
  - path normalization;
  - allowlist;
  - semantica de exit code;
  - links para politica e gate.

## 4. Validacao de resultado

### Estado do audit

Antes do hardening real (estado verdadeiro apos fix de path):
- `OK: 2 / Warning: 1 / Error: 0` (3 orfaos legados no mutirao)

Depois do allowlist operacional:
- `OK: 3 / Warning: 0 / Error: 0`

## 5. Gates executados

```bash
✅ npm run assets:audit
✅ npm run lint
✅ npm run type-check
✅ npm run test:unit (52/52)
✅ npm run build
✅ npm run verify (52/52 checks)
✅ npm run test:assets-smoke (4 unit + 9 e2e)
```

## 6. Resultado final

Pipeline de assets agora esta operacionalmente confiavel:
- ruido reduzido (falsos positivos removidos);
- legados controlados por allowlist explicita;
- severidade e bloqueio formalizados;
- health report executivo claro;
- base pronta para crescimento continuo sem bagunca.

## 7. Proximo ciclo recomendado (T47)

1. revisar allowlist trimestralmente e remover entradas vencidas.
2. consolidar workflow dedicado de assets audit em CI.
3. manter `npm run assets:audit` como gate obrigatorio para PRs com mudancas em `public/arcade/**`.
